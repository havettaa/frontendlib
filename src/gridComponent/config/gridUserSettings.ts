import { PaginatorData } from "../model/paginatorData";
import { FilterData } from "../model/filterData";

export class GridUserSettings {
    filterValues: [string, FilterData][];
    paginatorData: PaginatorData;

}